<div class="w-full shadow px-6 py-6 my-6 bg-gray-100">
  <div class="flex justify-between items-center mb-5">
    <h5 class="text-lg font-bold">Update your talent profile</h5>
    <%= link_to "Edit Profile", edit_employee_path(@employee), class: 'btn btn-outline btn-sm' %>
  </div>
  <div class="mt-5">
    <%= render "shared/messages", object: @employee %>
    <%= render partial: 'employees/new_ui/public_profile', locals: { employee: @employee } %>
  </div>
</div>

