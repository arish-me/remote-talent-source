<div class="input-group mb-3" data-controller="page-scroll">
  <%= form.label :content, t(".help"), class: "visually-hidden" %>
  <%= form.text_area :content, class: "form-control", rows: 4, required: true, autofocus: true, placeholder: t(".help"), data: { action: "keydown->forms--keyboard-submit#submit" } %>
  <button class="btn btn-primary" type="submit">Send</button>
</div>