<div class="bg-primary">
  <div class="py-3 px-3 lg-0">
    <div class="d-flex justify-content-between lg-justify-content-center gap-4 flex-wrap">
      <p class="ms-3 fw-medium text-white"><%= t(".title", email: helpers.current_user.email) %></p>
      <%= link_to t(".cta"), admin_impersonate_path,  data: {turbo_method: :delete}, class: "btn btn-primary" %>
    </div>
  </div>
</div>